import styled from '@emotion/styled';

export const Container = styled.ul`
  display: flex;
  flex-wrap: wrap;
  gap: 110px;
  padding-top: 120px;
  padding-left: 0;
  margin: 0;
  justify-content: center;
`;
